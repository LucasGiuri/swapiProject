export const getAllPlanets = (planets) => planets.items
export const getPlanetById = (planets) => planets.itemSelected
export const isLoadingPlanets = (planets) => planets.isLoading
export const getTotalPlanets = (planets) => planets.totalItems
export const getPlanetsNextPage = (planets) => planets.next
export const getPlanetsPreviousPage = (planets) => planets.previous
